# DocuNova 기술 스택 호환성 검토서

## 📋 문서 개요

**작성일**: 2025-10-30
**목적**: DocuNova SaaS 개발에 사용되는 모든 기술 스택의 호환성 검토 및 잠재적 이슈 파악
**결론**: 대부분 안정적이나, 일부 최신 버전 기술에서 주의 필요

---

## 🎯 전체 요약

### ✅ 호환성 상태

| 카테고리 | 상태 | 위험도 | 조치 필요 |
|---------|------|--------|----------|
| Frontend Core | ⚠️ 주의 | 중간 | 예 |
| Frontend UI Libraries | ✅ 안정 | 낮음 | 아니오 |
| Backend Core | ✅ 안정 | 낮음 | 아니오 |
| Vector DB & Embedding | ✅ 안정 | 낮음 | 아니오 |
| Document Processing | ✅ 안정 | 낮음 | 아니오 |
| LLM Integration | ⚠️ 주의 | 중간 | 예 |

### 🔴 주요 발견 사항

1. **React 19.2.0 + Next.js 16.0.0**: 최신 버전으로 일부 서드파티 라이브러리와 호환성 문제 가능
2. **Ollama 통합**: 안정성 및 에러 핸들링 강화 필요
3. **TypeScript 5.9.3**: React 19와 타입 정의 업데이트 필수

---

## 🖥️ 프론트엔드 기술 스택

### 1. Next.js 16.0.0 + React 19.2.0

#### ✅ 장점
- **공식 지원**: Next.js 16은 React 19.2를 공식 지원
- **최신 기능**: View Transitions, useEffectEvent, Activity 렌더링
- **성능 향상**: Turbopack 기본 탑재, 빌드 속도 개선
- **React Compiler**: 1.0 정식 릴리즈와 통합

#### ⚠️ 주의사항

**1. 서드파티 라이브러리 호환성**

```
문제: React 19에서 ref가 일반 prop으로 변경
영향: Ant Design 등 일부 UI 라이브러리 호환성 문제
해결: shadcn/ui 사용으로 이 문제 회피 (shadcn/ui는 React 19 호환)
```

**2. useEffectEvent 실험적 기능**

```
문제: useEffectEvent는 아직 실험 단계
영향: Radix UI (shadcn/ui 기반) 일부 컴포넌트에서 사용
해결: 프로덕션 빌드에서는 제외되므로 안전
```

**3. 타입 정의 업데이트 필수**

```typescript
// ⚠️ 반드시 최신 타입 정의 사용
"@types/react": "^19.2.2",
"@types/react-dom": "^19.2.2",
"@types/node": "^24.9.1"
```

**4. 동기 Request API 제거**

```typescript
// ❌ Next.js 16부터 불가능
const cookies = headers().get('cookie');  // 동기 호출

// ✅ 비동기로 변경 필수
const cookieStore = await cookies();
const value = cookieStore.get('name');
```

#### 📊 안정성 평가

| 항목 | 점수 | 비고 |
|-----|------|------|
| 핵심 기능 안정성 | 9/10 | Next.js + React 공식 통합 |
| 서드파티 호환성 | 7/10 | 일부 라이브러리 미지원 |
| 프로덕션 준비도 | 8/10 | 대부분 안정적 |
| 장기 지원 | 10/10 | 공식 LTS |

#### 🔧 권장 조치

**즉시 조치**:
1. ✅ shadcn/ui 사용 (이미 적용됨)
2. ✅ 최신 @types/react 사용 (이미 적용됨)
3. ⚠️ 동기 API 사용 여부 확인 필요

**모니터링**:
- React 19 관련 GitHub 이슈 추적
- shadcn/ui 업데이트 주기적 확인

---

### 2. TypeScript 5.9.3

#### ✅ 장점
- React 19와 완전 호환
- Next.js 16 공식 지원
- 최신 ECMAScript 기능 지원

#### ⚠️ 주의사항

```typescript
// tsconfig.json 필수 설정
{
  "compilerOptions": {
    "lib": ["ES2023", "DOM", "DOM.Iterable"],
    "jsx": "preserve",
    "strict": true,
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

#### 📊 안정성 평가

| 항목 | 점수 |
|-----|------|
| 핵심 기능 안정성 | 10/10 |
| Next.js 호환성 | 10/10 |
| React 19 타입 지원 | 9/10 |

#### 🔧 권장 조치
- ✅ 현재 버전 유지 (문제 없음)

---

### 3. Tailwind CSS 3.4.1

#### ✅ 장점
- Next.js 16 완전 호환
- 안정적이고 성숙한 버전
- 성능 최적화됨

#### 📊 안정성 평가

| 항목 | 점수 |
|-----|------|
| 안정성 | 10/10 |
| Next.js 통합 | 10/10 |
| 프로덕션 준비도 | 10/10 |

#### 🔧 권장 조치
- ✅ 현재 버전 유지 (완벽함)

---

### 4. UI 라이브러리

#### shadcn/ui 관련 패키지

```json
"lucide-react": "^0.547.0",          // ✅ 안정
"class-variance-authority": "^0.7.1", // ✅ 안정
"clsx": "^2.1.1",                     // ✅ 안정
"tailwind-merge": "^3.3.1",           // ✅ 안정
"tailwindcss-animate": "^1.0.7"       // ✅ 안정
```

#### ✅ 모든 패키지 호환성 확인됨

- React 19와 호환
- Next.js 16과 호환
- 프로덕션 사용 안정

#### 🔧 권장 조치
- ✅ 모든 패키지 현재 버전 유지

---

## 🔧 백엔드 기술 스택

### 1. FastAPI 0.115.0 + Python 3.11

#### ✅ 장점
- **완벽한 호환성**: Python 3.11 공식 지원
- **Pydantic 2.9.2**: 완전 호환 확인됨
- **안정성**: 수많은 프로덕션 사례
- **성능**: Python 3.11의 성능 향상 활용

#### 검증된 호환성

```python
# ✅ 검증된 조합
Python 3.11.1
FastAPI 0.115.0
Pydantic 2.9.2
Uvicorn 0.30.6
```

#### 📊 안정성 평가

| 항목 | 점수 |
|-----|------|
| 핵심 기능 안정성 | 10/10 |
| Pydantic 호환성 | 10/10 |
| 프로덕션 준비도 | 10/10 |
| 장기 지원 | 10/10 |

#### 🔧 권장 조치
- ✅ 현재 버전 완벽함 (변경 불필요)

---

### 2. Qdrant 1.12.1 + FastEmbed 0.3.2

#### ✅ 장점
- Qdrant와 FastEmbed는 동일 조직(Qdrant)에서 개발
- 공식 통합 지원
- 안정적인 버전

#### ⚠️ 주의사항

**1. Python 3.13 미지원**
```
현재: Python 3.11 사용 (✅ 안전)
주의: Python 3.13으로 업그레이드 시 FastEmbed 호환성 확인 필요
```

**2. HuggingFace 모델 다운로드**
```python
# 일부 환경에서 401 에러 발생 가능
# 해결: 로컬에서 사전 다운로드 또는 프록시 설정
```

#### 📊 안정성 평가

| 항목 | 점수 |
|-----|------|
| 핵심 기능 안정성 | 9/10 |
| 상호 호환성 | 10/10 |
| 프로덕션 준비도 | 9/10 |

#### 🔧 권장 조치

**즉시 조치**:
```python
# 에러 핸들링 강화
try:
    embedding = fastembed.TextEmbedding()
except Exception as e:
    log.error(f"FastEmbed 초기화 실패: {e}")
    # 폴백 메커니즘
```

**모니터링**:
- FastEmbed GitHub 이슈 확인
- 모델 다운로드 실패 모니터링

---

### 3. NumPy 1.26.4 + ONNX Runtime 1.17.3

#### ✅ 검증된 조합

```
NumPy 1.26.4  ←→  ONNX Runtime 1.17.3  ✅ 호환
                ↓
          FastEmbed 0.3.2  ✅ 호환
```

#### 📊 안정성 평가

| 항목 | 점수 |
|-----|------|
| 호환성 | 10/10 |
| 안정성 | 10/10 |

#### 🔧 권장 조치
- ✅ 현재 버전 유지 (완벽함)

---

### 4. 문서 처리 라이브러리

```python
pypdf==4.3.1           # ✅ 안정
PyMuPDF==1.24.10       # ✅ 안정
docx2txt==0.8          # ✅ 안정
python-docx==1.1.2     # ✅ 안정
openpyxl==3.1.5        # ✅ 안정
```

#### ✅ 모든 라이브러리 안정

- Python 3.11 호환
- 프로덕션 검증됨
- 활발한 유지보수

#### 🔧 권장 조치
- ✅ 현재 버전 유지

---

## 🤖 LLM 통합 (Ollama)

### 현재 상황

```
Backend (Port 8000)  →  Ollama (Port 11434)
                         llama3.1:8b
```

### ⚠️ 주요 우려사항

#### 1. 안정성 문제

```python
# 문제: Ollama 서버 다운 시 전체 시스템 영향
# 현재: 에러 핸들링 부족

# ❌ 취약한 코드
response = requests.post("http://localhost:11434/api/generate", ...)

# ✅ 개선 필요
async def query_llm_with_fallback(question: str):
    try:
        async with httpx.AsyncClient(timeout=30.0) as client:
            response = await client.post(
                "http://localhost:11434/api/generate",
                json={"model": "llama3.1:8b", "prompt": question}
            )
            return response.json()
    except httpx.TimeoutException:
        log.error("Ollama 타임아웃")
        raise HTTPException(status_code=504, detail="LLM 서버 응답 없음")
    except httpx.ConnectError:
        log.error("Ollama 연결 실패")
        raise HTTPException(status_code=503, detail="LLM 서버 연결 불가")
    except Exception as e:
        log.error(f"LLM 에러: {e}")
        raise HTTPException(status_code=500, detail="LLM 처리 실패")
```

#### 2. 성능 문제

```
문제: Ollama 응답 시간 불안정 (5초 ~ 30초)
영향: 사용자 경험 저하
해결:
  1. 스트리밍 응답 (이미 구현됨 ✅)
  2. 타임아웃 설정 (30초)
  3. 로딩 상태 명확히 표시
```

#### 3. 모델 관리

```bash
# 문제: 모델이 없을 때 에러
# 해결: 초기화 시 모델 존재 확인

ollama list | grep llama3.1:8b || ollama pull llama3.1:8b
```

### 📊 안정성 평가

| 항목 | 점수 | 비고 |
|-----|------|------|
| 핵심 기능 | 7/10 | 작동하지만 불안정 |
| 에러 핸들링 | 5/10 | 개선 필요 |
| 프로덕션 준비도 | 6/10 | 강화 필요 |

### 🔧 권장 조치

**필수 조치**:

1. **헬스체크 추가**
```python
@app.get("/api/health/llm")
async def llm_health():
    try:
        async with httpx.AsyncClient(timeout=5.0) as client:
            response = await client.get("http://localhost:11434/api/tags")
            return {"status": "healthy", "models": response.json()}
    except:
        return {"status": "unhealthy"}
```

2. **에러 메시지 개선**
```python
OLLAMA_ERROR_MESSAGES = {
    "connection": "AI 서버에 연결할 수 없습니다. 잠시 후 다시 시도해주세요.",
    "timeout": "AI 응답 시간이 초과되었습니다. 질문을 단순화해보세요.",
    "model_not_found": "AI 모델을 찾을 수 없습니다. 관리자에게 문의하세요.",
}
```

3. **재시도 로직**
```python
async def query_llm_with_retry(question: str, max_retries: int = 2):
    for attempt in range(max_retries):
        try:
            return await query_llm(question)
        except httpx.TimeoutException:
            if attempt == max_retries - 1:
                raise
            await asyncio.sleep(1 * (attempt + 1))
```

---

## 📊 전체 호환성 매트릭스

### 프론트엔드

| 기술 | 버전 | 상태 | 위험도 | 조치 |
|-----|------|------|--------|------|
| Next.js | 16.0.0 | ⚠️ 주의 | 중간 | 동기 API 확인 |
| React | 19.2.0 | ⚠️ 주의 | 중간 | 타입 최신 유지 |
| TypeScript | 5.9.3 | ✅ 안정 | 낮음 | 유지 |
| Tailwind CSS | 3.4.1 | ✅ 안정 | 낮음 | 유지 |
| shadcn/ui | Latest | ✅ 안정 | 낮음 | 유지 |
| lucide-react | 0.547.0 | ✅ 안정 | 낮음 | 유지 |

### 백엔드

| 기술 | 버전 | 상태 | 위험도 | 조치 |
|-----|------|------|--------|------|
| Python | 3.11 | ✅ 안정 | 낮음 | 유지 |
| FastAPI | 0.115.0 | ✅ 안정 | 낮음 | 유지 |
| Pydantic | 2.9.2 | ✅ 안정 | 낮음 | 유지 |
| Uvicorn | 0.30.6 | ✅ 안정 | 낮음 | 유지 |
| Qdrant | 1.12.1 | ✅ 안정 | 낮음 | 유지 |
| FastEmbed | 0.3.2 | ✅ 안정 | 낮음 | 모니터링 |
| Ollama | Latest | ⚠️ 주의 | 중간 | 에러 핸들링 강화 |

---

## 🔧 즉시 조치 필요 항목

### 🔴 우선순위 1 (즉시)

1. **Ollama 에러 핸들링 강화**
   - 위치: `backend/main.py` 또는 `backend/services/llm.py`
   - 작업: 타임아웃, 재시도, 폴백 메커니즘
   - 예상 시간: 2시간

2. **동기 Request API 사용 여부 확인**
   - 위치: Frontend 전체 코드
   - 작업: `headers()`, `cookies()` 등 비동기 변환
   - 예상 시간: 1시간

### 🟡 우선순위 2 (중요)

3. **LLM 헬스체크 엔드포인트 추가**
   - 위치: `backend/main.py`
   - 작업: `/api/health/llm` 엔드포인트 생성
   - 예상 시간: 30분

4. **FastEmbed 모델 다운로드 에러 핸들링**
   - 위치: `backend/services/embedding.py`
   - 작업: try-except 추가, 재다운로드 로직
   - 예상 시간: 30분

### 🟢 우선순위 3 (권장)

5. **React 19 타입 정의 최신 유지**
   - 위치: `package.json`
   - 작업: 정기적 업데이트 체크
   - 예상 시간: 15분 (주기적)

6. **문서화**
   - 위치: `README.md`, `docs/`
   - 작업: 알려진 이슈 및 해결방법 문서화
   - 예상 시간: 1시간

---

## 📋 대체 버전 권장 사항

### 보수적 접근 (안정성 최우선)

현재 최신 버전 대신 LTS 버전 사용을 고려한다면:

```json
// 프론트엔드 (더 안정적인 버전)
{
  "next": "15.1.0",           // Next.js 15 LTS
  "react": "18.3.1",          // React 18 LTS
  "react-dom": "18.3.1",
  "@types/react": "18.3.12",
  "@types/react-dom": "18.3.5"
}
```

#### ⚖️ 트레이드오프

**현재 버전 (Next.js 16 + React 19):**
- ✅ 최신 기능
- ✅ 성능 향상
- ⚠️ 일부 라이브러리 호환성 문제 가능
- ⚠️ 프로덕션 사례 적음

**LTS 버전 (Next.js 15 + React 18):**
- ✅ 매우 안정적
- ✅ 모든 라이브러리 호환
- ✅ 프로덕션 검증 완료
- ❌ 최신 기능 부족

### 🎯 권장 결론

**현재 버전 유지 권장**:
1. shadcn/ui 사용으로 React 19 호환성 문제 회피됨
2. Next.js 16 + React 19는 공식 지원
3. 최신 기능 활용 가능
4. 단, Ollama 통합 강화 필수

---

## 🔄 지속적 모니터링 계획

### 주간 체크리스트

- [ ] React 19 관련 GitHub 이슈 확인
- [ ] Next.js 16 업데이트 확인
- [ ] FastEmbed 릴리즈 노트 확인
- [ ] Ollama 안정성 모니터링

### 월간 체크리스트

- [ ] 전체 dependency 보안 취약점 스캔
- [ ] 성능 메트릭 리뷰
- [ ] 에러 로그 분석
- [ ] 사용자 피드백 검토

### 분기별 체크리스트

- [ ] 주요 버전 업데이트 검토
- [ ] 아키텍처 리뷰
- [ ] 부하 테스트
- [ ] 보안 감사

---

## 📚 참고 자료

### 공식 문서

- [Next.js 16 Release Notes](https://nextjs.org/blog/next-16)
- [React 19 Blog](https://react.dev/blog/2025/03/25/react-19)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [Qdrant Documentation](https://qdrant.tech/documentation/)
- [Ollama Documentation](https://ollama.ai/docs/)

### GitHub Issues

- [Next.js Issues](https://github.com/vercel/next.js/issues)
- [React Issues](https://github.com/facebook/react/issues)
- [FastEmbed Issues](https://github.com/qdrant/fastembed/issues)

---

## ✅ 최종 결론

### 전체 시스템 안정성: **8/10**

#### 강점
- ✅ 백엔드 스택 매우 안정적 (FastAPI + Python 3.11)
- ✅ Vector DB 스택 안정적 (Qdrant + FastEmbed)
- ✅ UI 라이브러리 호환성 확인됨 (shadcn/ui)

#### 개선 필요
- ⚠️ Ollama 통합 에러 핸들링 강화 필수
- ⚠️ Next.js 16 + React 19 동기 API 확인 필요

#### 프로덕션 준비도: **가능**

다음 조건 충족 시 프로덕션 배포 가능:
1. ✅ Ollama 에러 핸들링 구현 (우선순위 1)
2. ✅ 동기 API 사용 여부 확인 및 수정 (우선순위 1)
3. ✅ 통합 테스트 완료
4. ✅ 모니터링 체계 구축

---

## 📞 기술 지원

### 이슈 발생 시

1. **에러 로그 확인**
   - Frontend: 브라우저 콘솔
   - Backend: `app.log`

2. **헬스체크**
   ```bash
   # Backend
   curl http://localhost:8000/api/health

   # Ollama
   curl http://localhost:11434/api/tags

   # Qdrant
   curl http://localhost:6333/health
   ```

3. **버전 확인**
   ```bash
   # Frontend
   npm list next react

   # Backend
   pip list | grep -E "fastapi|qdrant|fastembed"
   ```

---

**이 검토서는 2025-10-30 기준으로 작성되었습니다.**
**정기적인 업데이트 및 모니터링이 필요합니다.** 🔄
